# Implementation of Different Noises with Three JS

.glsl files in the src folder contain different noise functions.

All three js files export a makeScene() function which is imported in App.js for rendering.
